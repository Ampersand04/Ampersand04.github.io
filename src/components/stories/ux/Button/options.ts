import { Options } from './Button.types'

export const options: Options = {
	type: ['default', 'primary', 'dashed', 'link', 'text'],
	color: ['blue', 'red', 'green'],
	sizes: ['small', 'middle', 'large']
}
